import React from "react";
import { Dimensions } from "react-native";
import { RFValue } from "../utils/RFValue";

const { width: WIDTH_SCREEN, height: HEIGHT_SCREEN } = Dimensions.get("screen");

export { WIDTH_SCREEN, HEIGHT_SCREEN };